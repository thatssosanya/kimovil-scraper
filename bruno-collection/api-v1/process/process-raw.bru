meta {
  name: Process Raw Extraction
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/process/raw
  body: json
  auth: none
}

body:json {
  {
    "source": "kimovil",
    "externalId": "samsung-galaxy-s24-ultra"
  }
}

docs {
  Extract raw data from cached HTML for a device.
  
  Requires HTML to already be cached.
}
